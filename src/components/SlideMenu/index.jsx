import React from 'react';
// elements
import { Grid, Button, BlurBox } from '../../elements';

const SlideMenu = props => {
  return (
    <BlurBox>
      <Grid />
    </BlurBox>
  );
};

SlideMenu.propTypes = {};

export default SlideMenu;
