import styled, { css } from 'styled-components';

const TabMenuWrapper = styled.ul`
  display: flex;

  li {
    cursor: pointer;
  }
`;

const SetAlignItemsButton = css`
  display: inline-flex;
  align-items: center;
`;

// export { TabMenuWrapper, SetAlignItemsButton };
